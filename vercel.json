{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs20.x" // Use the latest patch version syntax
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.js" }
  ]
}
